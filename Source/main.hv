
PointType := class
{
	pos := vec2f
	velocity := vec2f

	"=" := default
}

TWindow := class extends Window
{
	MousePos := vec2f
	
	MouseMoved := virtual !(double x, double y) -> void
	{
		MousePos.x = x
		MousePos.y = y
	}
}

main := !(char^[] args) -> int
{
	x := 800
	y := 800

	g := EngTools
	g.Init()

	win := g.window

	vInstance := vk

	shadersDB := ref g.shadersDB

	renderPass := vk.CreateRenderPass(![
			TRenderPassFrame(vk.SwapImageFormat, VK_IMAGE_LAYOUT_UNDEFINED,VK_IMAGE_LAYOUT_PRESENT_SRC_KHR)
		],false)

	ctx := GameContext

	ctx.pointsCount = 1024*32

	vk.initCreateStageMemory()

	g.CreateStorageBuffer("Particles",ctx.pointsCount*PointType->TypeSize,"vsr")

	ctx.vertData = g.vertStorage

	{
		verts := Vector.{PointType}
		verts.Resize(ctx.pointsCount)

		for it : verts
		{
			it.pos.x = RandInt(0..1000) / 1000 - 0.5
			it.pos.y = RandInt(0..1000) / 1000 - 0.5
			it.pos *= vec2f(2.0,2.0)
		}
		ctx.vertData.TransferData(verts)
	}

	layout := renderPass.CreateLayout(PointType,![],![],resultImageCount = 1,topology = "Points")

	shadersDB.SetInput("light",layout)

	drawShader := g.LoadShader2("ShadersBin/test.json")

	ctx.mainShader = shadersDB.LoadShader("ShadersBin/test.json")

	mouseMoveShader := g.LoadComputeShader("ShadersBin/MouseMove.comp")
	moveShader := g.LoadComputeShader("ShadersBin/Move.comp")

	baseLayout := g.shaderLayout

	g.Hack()

	// if #DebugMode()
		shadersDB.SetFolderHotload("ShadersBin/")
	
	cmds := TVkCommandBuffer(vk,vk.itDrawCmdPool2)

	oldTime := GetSteadyTime()

	while not win.ShouldClose()
	{
		// if #DebugMode()
			shadersDB.UpdateShaders(vk)

		surfaceIndex := u32
		drawRes := false
		@work vk.DrawGetImage(surfaceIndex&,drawRes&)

		thisTime := GetSteadyTime()
		deltaTime := thisTime - oldTime
		oldTime = thisTime

		g.DeltaTime = deltaTime
		g.MousePosition.x = win.MousePos.x / x
		g.MousePosition.y = win.MousePos.y / y

		cmds.Reset()
		cmds.Start()

		if buttons['q'] or MouseButtons[0]
			g.ExecuteComputeShader(cmds,mouseMoveShader,ctx.pointsCount)
		g.ExecuteComputeShader(cmds,moveShader,ctx.pointsCount)

		DrawParticles(ctx,cmds,surfaceIndex,x,y,g)

		cmds.Stop()
		cmds.Submit(vk.DrawQueue,null)
		vk.Funcs.vkQueueWaitIdle(vk.DrawQueue)

		vk.DrawPresentImage(surfaceIndex)

		win.PollEvents()
	}
	win.DestroyWindow()
	return 0
}
